import { listPrint } from 'epikillus/helpers/list-print';
import { module, test } from 'qunit';

module('Unit | Helper | list print');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listPrint([42]);
  assert.ok(result);
});
